# Copy to frontend/.env for local development
#   cp .env.example .env

# API base URL (no trailing slash)
# Leave empty for local dev — Vite proxies /api to http://localhost:8080
VITE_API_URL=

# Production example (set in Vercel before build):
# VITE_API_URL=https://guessify-api.onrender.com
